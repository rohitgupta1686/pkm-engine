---
phase: 07-scheduled-jobs-guardrails
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - docs/GUARDRAILS.md
autonomous: false
requirements: [GUARD-04, GUARD-05, GUARD-07]

must_haves:
  truths:
    - "GitHub Actions spending limit is confirmed $0 / fail-closed for the vault repo (and any private mirror)"
    - "OpenAI project/organization monthly spend cap is set in the OpenAI console (GUARD-05, reconciled from Anthropic)"
    - "A second git remote exists and secrets.BACKUP_REMOTE_URL is configured in pkm-engine so the nightly backup push succeeds"
    - "CF_ACCOUNT_ID + CF_API_TOKEN (scoped Workers AI:Read + Vectorize:Edit) are configured as GitHub Actions secrets so CI embedding works"
    - "A manual nightly run (workflow_dispatch) regenerates dashboard.md, writes a lint line to log.md, and pushes to the backup remote"
  artifacts:
    - path: "docs/GUARDRAILS.md"
      provides: "Checkboxes for each human-confirmed guardrail, updated by the operator during this plan"
      contains: "GUARD-04"
  key_links:
    - from: "GitHub Actions secrets (BACKUP_REMOTE_URL, CF_ACCOUNT_ID, CF_API_TOKEN)"
      to: ".github/workflows/ingest.yml backup + backfill steps"
      via: "secrets.* references resolved at run time"
      pattern: "secrets.BACKUP_REMOTE_URL"
---

<objective>
Execute the human-side guardrail confirmations that Claude cannot perform (GUARD-04, GUARD-05, GUARD-07, and the deferred CF-credentials setup), then verify a manual nightly run produces every Phase 7 artifact end-to-end.

Purpose: GUARD-04 (GH spending limit), GUARD-05 (OpenAI monthly cap), the second backup remote + its secret (GUARD-07), and the CF embedding credentials are external console/account configurations. They are prerequisites for the automated workflow (Plan 04) to actually function and for the success criteria "spending limit confirmed $0" and "backup push succeeds" to be true. This plan pauses for the operator to do that work and confirm it.
Output: completed checkboxes in docs/GUARDRAILS.md + a verified end-to-end nightly run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/SECRETS.md
@docs/GUARDRAILS.md
@.github/workflows/ingest.yml
@.planning/phases/07-scheduled-jobs-guardrails/07-04-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Configure external guardrails + secrets (operator actions)</name>
  <files>docs/GUARDRAILS.md</files>
  <read_first>
    - docs/GUARDRAILS.md (the runbook written in Plan 04 — the exact procedures to follow)
    - docs/SECRETS.md (existing secrets inventory — what is already configured vs. what is new)
    - .planning/STATE.md (OpenAI backend reconciliation; CF creds were used live in Phase 5 worker-clip deploy)
    - .github/workflows/ingest.yml (which secret names the workflow references: BACKUP_REMOTE_URL, CF_ACCOUNT_ID, CF_API_TOKEN, VAULT_PAT, OPENAI_API_KEY, TURSO_URL, TURSO_TOKEN)
  </read_first>
  <what-built>
    Plan 04 added the automated guardrail steps to ingest.yml (backfill-embeds, lint, dashboard, 80%-alert, backup-push) and docs/GUARDRAILS.md documents the procedures. The workflow references secrets.BACKUP_REMOTE_URL, secrets.CF_ACCOUNT_ID, and secrets.CF_API_TOKEN, none of which exist as GitHub Actions secrets yet. GUARD-04 and GUARD-05 also require console-side confirmation that no automated step can perform.
  </what-built>
  <action>Operator performs the four external configurations detailed in &lt;how-to-verify&gt; (GH spending limit, OpenAI monthly cap, second backup remote + BACKUP_REMOTE_URL secret, CF_ACCOUNT_ID + CF_API_TOKEN secrets) and ticks the corresponding checkboxes in docs/GUARDRAILS.md with a confirmation date and no secret values.</action>
  <how-to-verify>
    Operator performs each item and checks its box in docs/GUARDRAILS.md:

    1. GUARD-04 — GitHub Actions spending limit:
       - For pkm-engine (public): confirm it is public → unlimited free minutes (no spending limit needed). Record this in the doc.
       - For pkm-vault (private) and any future private mirror: go to GitHub account Settings → Billing & plans → Spending limit → set Actions spending limit to $0 (fail-closed). Confirm a run exceeding the free quota would be halted, not billed.

    2. GUARD-05 — OpenAI monthly spend cap (reconciled from Anthropic):
       - Log in to the OpenAI console → Settings → Billing → set a monthly budget / hard spend limit (fail-closed). Record the cap value (not the key) in docs/GUARDRAILS.md.
       - Confirm the per-run code caps are in place: PKM_RUN_COST_CAP_USD (default 0.50) and PKM_RUN_TOKEN_CAP (default 200000) in pkm/batch.py — already enforced, no action beyond confirmation.

    3. GUARD-07 — Second git remote + BACKUP_REMOTE_URL secret:
       - Create a second private GitHub repo (e.g. pkm-vault-backup) OR identify an existing off-site git host.
       - Create a fine-grained PAT (or deploy token) with contents:write on the backup repo ONLY.
       - Construct the full https push URL with the credential embedded: https://x-access-token:<TOKEN>@github.com/<owner>/pkm-vault-backup.git
       - In pkm-engine GitHub Settings → Secrets and variables → Actions → New repository secret: name BACKUP_REMOTE_URL, value = the full URL above. (GitHub masks the secret in logs.)

    4. Deferred CF credentials (Phase 6 Wave 3 gap):
       - In the Cloudflare dashboard → My Profile → API Tokens → Create Token → Custom token with scopes: Workers AI:Read + Vectorize:Edit (and Account:Read for account id lookup).
       - Copy CF_ACCOUNT_ID (Cloudflare dashboard right sidebar) and the new CF_API_TOKEN.
       - In pkm-engine GitHub Settings → Secrets → add CF_ACCOUNT_ID and CF_API_TOKEN.

    5. Update docs/GUARDRAILS.md: tick the checkbox beside each of the four items above and record the confirmation date. Do NOT record any secret/token values — only that they are set.
  </how-to-verify>
  <verify>
    <automated>test -f docs/GUARDRAILS.md && grep -q "\- \[x\]" docs/GUARDRAILS.md && ! grep -Eq "ghp_|github_pat_|x-access-token:|api\.cloudflare" docs/GUARDRAILS.md && echo "checkboxes ticked, no secrets committed"</automated>
  </verify>
  <resume-signal>Type "configured" once all four items are done and docs/GUARDRAILS.md checkboxes are ticked, or describe which item is blocked.</resume-signal>
  <acceptance_criteria>
    - docs/GUARDRAILS.md contains ticked checkboxes (e.g. "- [x]") for GUARD-04, GUARD-05, GUARD-07 backup remote, and the CF-credentials setup
    - docs/GUARDRAILS.md contains a confirmation date string for each
    - docs/GUARDRAILS.md contains NO secret/token values (grep for "ghp_\|github_pat_\|x-access-token:\|api.cloudflare" returns nothing)
    - The four GitHub Actions secrets (BACKUP_REMOTE_URL, CF_ACCOUNT_ID, CF_API_TOKEN, plus existing VAULT_PAT/OPENAI_API_KEY/TURSO_URL/TURSO_TOKEN) are present in the repo's Actions secrets list (operator confirms; visible as set, value masked)
  </acceptance_criteria>
  <done>All four external guardrails configured; secrets added to GitHub; docs/GUARDRAILS.md checkboxes ticked with a date and no secret values committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify a manual nightly run produces all Phase 7 artifacts</name>
  <files>docs/GUARDRAILS.md</files>
  <read_first>
    - .github/workflows/ingest.yml (the full nightly flow — know which steps to look for in the Actions run log)
    - docs/GUARDRAILS.md (success criteria mapping)
    - .planning/ROADMAP.md Phase 7 success criteria (the 5 criteria this run must satisfy)
  </read_first>
  <what-built>
    With the secrets configured in Task 1, the workflow's nightly steps (backfill-embeds, lint, dashboard, 80%-alert, backup push) are now functional. This task confirms they actually produce the expected artifacts end-to-end.
  </what-built>
  <action>Operator triggers a workflow_dispatch run of the ingest workflow, observes each nightly step in the Actions log, confirms the 5 ROADMAP Phase 7 success criteria, and appends a Verification section (run URL + per-criterion PASS/FAIL) to docs/GUARDRAILS.md per &lt;how-to-verify&gt;.</action>
  <how-to-verify>
    1. Trigger a manual run: GitHub → pkm-engine → Actions → "ingest" workflow → Run workflow (workflow_dispatch). Wait for completion (< 10 min per timeout).

    2. In the run logs, confirm each step ran:
       - "Run batch ingest" — completed (deduped or wrote)
       - "Backfill embeddings" — ran; if CF creds are now set, embeddings_meta row count in Turso should cover all claims (operator can run `SELECT COUNT(*) FROM embeddings_meta` and `SELECT COUNT(*) FROM claims` via Turso and confirm they match)
       - "Lint vault" — ran (continue-on-error, so yellow is acceptable for a dirty vault); check log.md for a "lint ok" or "lint FAIL ..." line
       - "Regenerate dashboard" — ran; vault/dashboard.md was updated
       - "80% Actions-minutes alert" — ran (no warning expected at current low usage)
       - "Backup push to second remote" — ran; confirm the backup remote's main branch advanced (git log on the backup repo, or GitHub web UI shows the latest commit)

    3. Verify the 5 ROADMAP Phase 7 success criteria:
       1. dashboard.md regenerated with current counts — open pkm-vault/dashboard.md, confirm it has today's generated-at timestamp and non-zero counts
       2. Lint wrote to log.md (empty failures = clean vault) — open pkm-vault/log.md, confirm a recent "lint ..." line
       3. 80% check ran — visible in the run log (the step executed)
       4. GH spending limit confirmed $0 — done in Task 1 (documented)
       5. Backup push succeeded — backup remote main branch matches pkm-vault main HEAD

    4. Append a "Verification" section to docs/GUARDRAILS.md recording: run URL, date, and PASS/FAIL for each of the 5 criteria.
  </how-to-verify>
  <verify>
    <automated>grep -q "## Verification" docs/GUARDRAILS.md && grep -q "PASS" docs/GUARDRAILS.md && echo "verification section recorded"</automated>
  </verify>
  <resume-signal>Type "approved" with the run URL if all 5 success criteria pass, or list the failing criterion.</resume-signal>
  <acceptance_criteria>
    - docs/GUARDRAILS.md contains a "## Verification" section with a GitHub Actions run URL and a PASS/FAIL line for each of the 5 Phase 7 success criteria
    - pkm-vault/dashboard.md has a generated-at timestamp from the verification run and shows non-zero counts
    - pkm-vault/log.md contains a "lint" line from the verification run
    - The backup remote's main branch HEAD matches pkm-vault's main HEAD (operator confirms)
    - If any criterion FAILS, it is recorded with a reason and the corresponding Plan 01-04 plan is revisited
  </acceptance_criteria>
  <done>All 5 Phase 7 success criteria verified PASS via a real workflow_dispatch run; results recorded in docs/GUARDRAILS.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator console → GitHub secrets | human creates scoped tokens and enters them as masked secrets |
| backup remote | new trust surface introduced this phase; credential scope must be minimal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Spoofing | backup PAT scope | mitigate | Task 1 requires the backup PAT be fine-grained, contents:write on the backup repo ONLY — not the production vault; operator confirms scope in the checkpoint |
| T-07-05-02 | Information disclosure | secrets committed to docs | mitigate | acceptance criteria explicitly grep docs/GUARDRAILS.md for token patterns (ghp_, github_pat_, x-access-token:, api.cloudflare) and require zero matches |
| T-07-05-03 | Elevation of privilege | CF API token scope | mitigate | Task 1 requires CF_API_TOKEN scoped to Workers AI:Read + Vectorize:Edit only (matches Phase 5 worker-clip token discipline); no account-wide token |
| T-07-05-04 | Repudiation | guardrail confirmation | mitigate | Task 1 + Task 2 record confirmation dates + run URL in docs/GUARDRAILS.md so the state is auditable |
</threat_model>

<verification>
- docs/GUARDRAILS.md has ticked checkboxes for all four operator items + a Verification section with run URL and 5 PASS lines
- No secret material committed (grep returns nothing)
- Backup remote HEAD matches vault HEAD
</verification>

<success_criteria>
- GUARD-04, GUARD-05, GUARD-07 confirmed by the operator and recorded
- Deferred CF-credentials gap closed (CI embedding now functional)
- A real nightly run verifies all 5 Phase 7 success criteria
</success_criteria>

<output>
Create .planning/phases/07-scheduled-jobs-guardrails/07-05-SUMMARY.md when done
</output>