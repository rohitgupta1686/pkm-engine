---
phase: 02-core-agents
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - pkm/agents/summarizer_agent.py
  - pkm/agents/concept_extractor.py
  - pkm/schemas/agent_io.py
  - tests/test_agents.py
  - tests/fixtures/golden_summarizer_output.json
  - tests/fixtures/golden_extractor_output.json
autonomous: true
requirements: [AGNT-02, AGNT-03, AGNT-05, AGNT-06]

must_haves:
  truths:
    - "SummarizerAgent returns a validated SummarizerOutput pydantic instance"
    - "ConceptExtractor returns a validated ConceptExtractorOutput pydantic instance"
    - "Both agents use SONNET model"
    - "Every KeyClaim in SummarizerOutput has chunk_id set (string); claims with null chunk_id have confidence <= 0.5"
    - "Golden-fixture tests pass for both agents using mocked LLMClient"
    - "agent_runs rows written for both agents (AGNT-06 via LLMClient mock)"
    - "Schema-invalid mock response triggers the repair-retry path (tested via LLMClient directly)"
  artifacts:
    - path: "pkm/agents/summarizer_agent.py"
      provides: "SummarizerAgent(BaseAgent) returning SummarizerOutput"
      contains: "class SummarizerAgent(BaseAgent)"
    - path: "pkm/agents/concept_extractor.py"
      provides: "ConceptExtractor(BaseAgent) returning ConceptExtractorOutput"
      contains: "class ConceptExtractor(BaseAgent)"
    - path: "pkm/schemas/agent_io.py"
      provides: "ConceptExtractorOutput added alongside existing models"
      contains: "class ConceptExtractorOutput"
    - path: "tests/fixtures/golden_summarizer_output.json"
      provides: "Valid SummarizerOutput JSON for golden test"
    - path: "tests/fixtures/golden_extractor_output.json"
      provides: "Valid ConceptExtractorOutput JSON for golden test"
  key_links:
    - from: "pkm/agents/summarizer_agent.py"
      to: "pkm/schemas/agent_io.py"
      via: "output_schema = SummarizerOutput"
      pattern: "SummarizerOutput"
    - from: "pkm/agents/concept_extractor.py"
      to: "pkm/schemas/agent_io.py"
      via: "output_schema = ConceptExtractorOutput"
      pattern: "ConceptExtractorOutput"
---

<objective>
Implement SummarizerAgent and ConceptExtractor as BaseAgent subclasses with pydantic-validated
structured output. Add ConceptExtractorOutput to agent_io.py. Write golden-fixture tests for both
agents and a repair-retry test that proves AGNT-05 fires.

Purpose: These two agents do the core semantic work of the pipeline. Both return rich pydantic
models that the pipeline stores in Turso (Phase 3). Validating them against golden fixtures now
means Phase 3 can trust the schemas.

Output: 2 agent files, updated agent_io.py, 2 fixture files, extended test_agents.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/RohitGupta/Library/Mobile Documents/com~apple~CloudDocs/Personal Knowledge Management/.planning/PROJECT.md
@/Users/RohitGupta/Library/Mobile Documents/com~apple~CloudDocs/Personal Knowledge Management/.planning/ROADMAP.md

<interfaces>
From pkm/schemas/agent_io.py (existing — do NOT delete any existing class):
  class KeyClaim(BaseModel):
      statement: str
      subject: str | None = None
      predicate: str | None = None
      object: str | None = None
      claim_type: Literal["fact","opinion","prediction","definition","causal","statistic"]
      chunk_id: str
      confidence: float = Field(ge=0, le=1)

  class SummarizerOutput(BaseModel):
      thesis: str
      key_claims: list[KeyClaim]
      caveats: list[str]
      summary_confidence: float = Field(ge=0, le=1)

  class GraphNode(BaseModel): ...   (keep as-is)
  class GraphRelationship(BaseModel): ...   (keep as-is)
  class KGAgentOutput(BaseModel): ...   (keep as-is)

From pkm/agents/base.py (02-01):
  BaseAgent.run(self, llm_client, input_text, source_id=None, extra_messages=None) -> BaseModel

From tests/test_agents.py (02-02):
  build_mock_llm_client(conn, result_text_or_model, tokens_in, tokens_out) helper
  Placeholder: class TestSummarizerAgent: pass
  Placeholder: class TestConceptExtractor: pass

IMPORTANT: chunk_id field in KeyClaim is typed str (not Optional[str]). The spec says "null + confidence <= 0.5"
means the string value "null" (not Python None). Agent prompts instruct the LLM to use "null" as the string
when no chunk is traceable — this is already handled in the prompt file. The pydantic model keeps chunk_id: str.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SummarizerAgent + ConceptExtractor + ConceptExtractorOutput schema</name>
  <files>pkm/agents/summarizer_agent.py, pkm/agents/concept_extractor.py, pkm/schemas/agent_io.py</files>
  <read_first>
    - /Users/RohitGupta/code/pkm-engine/pkm/agents/base.py (BaseAgent ClassVar attributes to replicate)
    - /Users/RohitGupta/code/pkm-engine/pkm/schemas/agent_io.py (existing models — must preserve all)
    - /Users/RohitGupta/code/pkm-engine/pkm/llm/models.py (SONNET constant)
  </read_first>
  <behavior>
    - SummarizerAgent.role == "summarizer_agent"
    - SummarizerAgent.model == SONNET ("claude-sonnet-4-6")
    - SummarizerAgent.output_schema is SummarizerOutput
    - ConceptExtractor.role == "concept_extractor"
    - ConceptExtractor.model == SONNET
    - ConceptExtractor.output_schema is ConceptExtractorOutput
    - ConceptExtractorOutput has fields: claims (list[KeyClaim]) and concept_matches (list[ConceptMatch])
    - ConceptMatch has fields: concept_name (str), claim_indices (list[int]), confidence (float ge=0 le=1)
    - Instantiating both agents does not raise
  </behavior>
  <action>
    Edit pkm/schemas/agent_io.py — add two new classes AFTER the existing imports and BEFORE any usage,
    preserving all existing classes (KeyClaim, SummarizerOutput, GraphNode, GraphRelationship, KGAgentOutput):

      class ConceptMatch(BaseModel):
          concept_name: str
          claim_indices: list[int]   # indices into the claims list
          confidence: float = Field(ge=0, le=1)

      class ConceptExtractorOutput(BaseModel):
          claims: list[KeyClaim]
          concept_matches: list[ConceptMatch]

    Create pkm/agents/summarizer_agent.py:
      Import BaseAgent from pkm.agents.base, SummarizerOutput from pkm.schemas.agent_io, SONNET from pkm.llm.models.
      class SummarizerAgent(BaseAgent):
          role = "summarizer_agent"
          model = SONNET
          prompt_template = "summarize.v1.md"
          prompt_version = "v1"
          input_schema = None
          output_schema = SummarizerOutput
          memory_tier = "working"

    Create pkm/agents/concept_extractor.py:
      Import BaseAgent from pkm.agents.base, ConceptExtractorOutput from pkm.schemas.agent_io, SONNET from pkm.llm.models.
      class ConceptExtractor(BaseAgent):
          role = "concept_extractor"
          model = SONNET
          prompt_template = "extract_claims.v1.md"
          prompt_version = "v1"
          input_schema = None
          output_schema = ConceptExtractorOutput
          memory_tier = "working"
  </action>
  <verify>
    <automated>cd /Users/RohitGupta/code/pkm-engine && python -c "
from pkm.agents.summarizer_agent import SummarizerAgent
from pkm.agents.concept_extractor import ConceptExtractor
from pkm.schemas.agent_io import ConceptExtractorOutput, ConceptMatch, SummarizerOutput
from pkm.llm.models import SONNET
assert SummarizerAgent.model == SONNET
assert ConceptExtractor.model == SONNET
assert ConceptExtractor.output_schema is ConceptExtractorOutput
# Verify existing models not broken
from pkm.schemas.agent_io import KGAgentOutput, GraphNode, GraphRelationship, KeyClaim
print('All agent and schema imports OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - pkm/agents/summarizer_agent.py contains "class SummarizerAgent(BaseAgent)"
    - pkm/agents/concept_extractor.py contains "class ConceptExtractor(BaseAgent)"
    - pkm/schemas/agent_io.py contains "class ConceptExtractorOutput" and "class ConceptMatch"
    - pkm/schemas/agent_io.py still contains "class KGAgentOutput" (not removed)
    - python -c "from pkm.schemas.agent_io import ConceptExtractorOutput, KGAgentOutput" exits 0
    - SummarizerAgent().model == "claude-sonnet-4-6"
    - ConceptExtractor().output_schema.__name__ == "ConceptExtractorOutput"
  </acceptance_criteria>
  <done>SummarizerAgent and ConceptExtractor defined; ConceptExtractorOutput + ConceptMatch added to agent_io.py; all existing schemas preserved</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Golden-fixture tests for Summarizer + ConceptExtractor + repair-retry test</name>
  <files>tests/test_agents.py, tests/fixtures/golden_summarizer_output.json, tests/fixtures/golden_extractor_output.json</files>
  <read_first>
    - /Users/RohitGupta/code/pkm-engine/tests/test_agents.py (existing file from 02-02 — extend it, do NOT rewrite from scratch)
    - /Users/RohitGupta/code/pkm-engine/tests/conftest.py (db_conn fixture)
    - /Users/RohitGupta/code/pkm-engine/pkm/llm/client.py (_extract_result repair-retry logic to understand how to test it)
  </read_first>
  <behavior>
    - test_summarizer_agent_golden: mock returns golden SummarizerOutput instance; result is SummarizerOutput; agent_runs row written
    - test_summarizer_chunk_id_rule: any KeyClaim with chunk_id == "null" has confidence <= 0.5 (tested on the golden fixture)
    - test_concept_extractor_golden: mock returns golden ConceptExtractorOutput; result is ConceptExtractorOutput; agent_runs row written
    - test_repair_retry_fires: LLMClient._extract_result is called with a malformed tool response; first ValidationError triggers the repair path; if repair also fails, ValidationError propagates (test the propagation path using a mock that always fails validation)
    - All new tests are added to the EXISTING test_agents.py (do not recreate the file)
  </behavior>
  <action>
    Create tests/fixtures/golden_summarizer_output.json — a JSON object matching SummarizerOutput schema:
      {
        "thesis": "Taking smart notes through atomic, linked ideas creates emergent knowledge.",
        "key_claims": [
          {
            "statement": "The slip-box method relies on writing atomic notes that each capture a single idea.",
            "subject": "slip-box method",
            "predicate": "relies on",
            "object": "atomic notes",
            "claim_type": "fact",
            "chunk_id": "chk_abc123_001",
            "confidence": 0.9
          },
          {
            "statement": "Writing forces clarification of thought.",
            "subject": "writing",
            "predicate": "forces",
            "object": "clarification of thought",
            "claim_type": "causal",
            "chunk_id": "null",
            "confidence": 0.4
          }
        ],
        "caveats": ["Based on a single source author's methodology."],
        "summary_confidence": 0.85
      }

    Create tests/fixtures/golden_extractor_output.json — a JSON object matching ConceptExtractorOutput:
      {
        "claims": [
          {
            "statement": "Atomic notes linked together surface emergent patterns.",
            "subject": "atomic notes",
            "predicate": "surface",
            "object": "emergent patterns",
            "claim_type": "causal",
            "chunk_id": "chk_abc123_001",
            "confidence": 0.85
          }
        ],
        "concept_matches": [
          {"concept_name": "Atomic Notes", "claim_indices": [0], "confidence": 0.9},
          {"concept_name": "Knowledge Management", "claim_indices": [0], "confidence": 0.75}
        ]
      }

    Edit tests/test_agents.py — REPLACE the placeholder "class TestSummarizerAgent: pass" with
    a real test class, and REPLACE "class TestConceptExtractor: pass" similarly. Keep TestReaderAgent
    and TestKGAgent untouched.

    class TestSummarizerAgent:
      def test_summarizer_agent_golden(self, db_conn):
        Load golden_summarizer_output.json; parse with SummarizerOutput.model_validate(json.loads(text)).
        Build mock returning the parsed model instance.
        agent = SummarizerAgent(); result = agent.run(mock, input_text="some text")
        Assert isinstance(result, SummarizerOutput).
        Assert result.thesis is a non-empty str.
        Row = db_conn.execute("SELECT status FROM agent_runs WHERE agent='summarizer_agent'").fetchone()
        Assert row[0] == "ok".

      def test_summarizer_chunk_id_rule(self, db_conn):
        Load golden fixture, parse. For each claim in result.key_claims:
          if claim.chunk_id == "null": assert claim.confidence <= 0.5
        (This verifies the data contract from AGNT-02/spec AD-6)

    class TestConceptExtractor:
      def test_concept_extractor_golden(self, db_conn):
        Load golden_extractor_output.json; parse with ConceptExtractorOutput.model_validate(json.loads(text)).
        Build mock; agent = ConceptExtractor(); result = agent.run(mock, input_text="some text").
        Assert isinstance(result, ConceptExtractorOutput).
        Assert len(result.claims) >= 1.
        Assert len(result.concept_matches) >= 1.
        Row = db_conn.execute("SELECT status FROM agent_runs WHERE agent='concept_extractor'").fetchone()
        Assert row[0] == "ok".

    Add inside TestSummarizerAgent a repair-retry test:
      def test_repair_retry_propagates_on_double_failure(self, db_conn):
        Import LLMClient from pkm.llm.client.
        Create a real LLMClient(db_conn, api_key="test-key").
        Patch anthropic.Anthropic.messages.create (via unittest.mock.patch) to return a fake response
        where the tool_use block input is {"bad_field": "wrong"} (will fail SummarizerOutput validation).
        Set the patch to return this malformed response both times (initial + repair call).
        Call llm_client.call(agent_name="summarizer_agent", model=SONNET,
          prompt_version="v1", messages=[{"role":"user","content":"test"}],
          input_text="test", output_schema=SummarizerOutput).
        Assert pytest.raises(pydantic.ValidationError).
        NOTE: To mock the Anthropic client, patch "pkm.llm.client.anthropic.Anthropic" and configure
        the mock's .messages.create.return_value to a MagicMock with:
          .usage.input_tokens = 5
          .usage.output_tokens = 5
          .content = [MagicMock(type="tool_use", name="structured_output", input={"bad_field": "wrong"})]
  </action>
  <verify>
    <automated>cd /Users/RohitGupta/code/pkm-engine && python -m pytest tests/test_agents.py::TestSummarizerAgent tests/test_agents.py::TestConceptExtractor -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/golden_summarizer_output.json exists and parses to valid SummarizerOutput (verified by test)
    - tests/fixtures/golden_extractor_output.json exists and parses to valid ConceptExtractorOutput (verified by test)
    - pytest tests/test_agents.py::TestSummarizerAgent exits 0 with 3 passed (golden, chunk_id_rule, repair_retry)
    - pytest tests/test_agents.py::TestConceptExtractor exits 0 with 1 passed
    - golden_summarizer_output.json contains a claim with "chunk_id": "null" and "confidence": 0.4 (verifies chunk_id rule data)
    - test_agents.py still contains TestReaderAgent (not overwritten)
  </acceptance_criteria>
  <done>Summarizer and ConceptExtractor golden tests pass; chunk_id confidence rule tested; repair-retry propagation tested; all 4 test classes present in test_agents.py</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM response → pydantic validation | Untrusted JSON from API; pydantic is the validation gate |
| golden fixture files → test assertions | Developer-controlled JSON; no untrusted data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | ConceptExtractorOutput.concept_matches | mitigate | claim_indices are list[int] — pydantic will reject string indices or out-of-range values |
| T-02-06 | Information Disclosure | SummarizerOutput.key_claims | accept | Claims are extracted from user-owned source text; no external data leakage |
| T-02-07 | Elevation of Privilege | chunk_id="null" confidence bypass | mitigate | test_summarizer_chunk_id_rule asserts the <= 0.5 invariant; enforced as data contract |
| T-02-SC | Tampering | pip installs | accept | No new packages; pydantic already installed |
</threat_model>

<verification>
cd /Users/RohitGupta/code/pkm-engine && python -m pytest tests/test_agents.py::TestReaderAgent tests/test_agents.py::TestSummarizerAgent tests/test_agents.py::TestConceptExtractor -v
# Expected: 6 passed, 0 failed
</verification>

<success_criteria>
- SummarizerAgent: role="summarizer_agent", model=SONNET, output_schema=SummarizerOutput
- ConceptExtractor: role="concept_extractor", model=SONNET, output_schema=ConceptExtractorOutput
- ConceptExtractorOutput and ConceptMatch added to pkm/schemas/agent_io.py; existing schemas preserved
- Golden fixture tests pass for both agents
- chunk_id == "null" → confidence <= 0.5 assertion passes on golden fixture
- Repair-retry double-failure propagates ValidationError (tested)
- pytest TestReaderAgent + TestSummarizerAgent + TestConceptExtractor: 6 total passed
</success_criteria>

<output>
Create /Users/RohitGupta/Library/Mobile Documents/com~apple~CloudDocs/Personal Knowledge Management/.planning/phases/02-core-agents/02-03-SUMMARY.md when done.
</output>
