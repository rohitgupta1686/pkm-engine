---
phase: 02-core-agents
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - pkm/agents/reader_agent.py
  - tests/test_agents.py
  - tests/fixtures/golden_reader_output.md
autonomous: true
requirements: [AGNT-01, AGNT-06]

must_haves:
  truths:
    - "ReaderAgent(BaseAgent) normalizes raw Markdown bytes to clean Markdown with YAML front matter"
    - "ReaderAgent uses HAIKU model"
    - "ReaderAgent golden-fixture test passes: fixture input → expected front matter fields present"
    - "Each ReaderAgent call writes a row to agent_runs (tokens_in, tokens_out, cost_usd)"
  artifacts:
    - path: "pkm/agents/reader_agent.py"
      provides: "ReaderAgent class extending BaseAgent"
      contains: "class ReaderAgent(BaseAgent)"
    - path: "tests/fixtures/golden_reader_output.md"
      provides: "Expected output shape for golden fixture test"
    - path: "tests/test_agents.py"
      provides: "Golden-fixture test for ReaderAgent (and placeholder test class stubs for plans 03-04)"
      contains: "test_reader_agent_golden"
  key_links:
    - from: "pkm/agents/reader_agent.py"
      to: "pkm/agents/base.py"
      via: "class ReaderAgent(BaseAgent)"
      pattern: "BaseAgent"
    - from: "tests/test_agents.py"
      to: "pkm/agents/reader_agent.py"
      via: "ReaderAgent import + db_conn fixture"
      pattern: "ReaderAgent"
---

<objective>
Implement ReaderAgent as the first concrete BaseAgent subclass, then write the golden-fixture
test infrastructure that all four agents will share.

Purpose: Validates the BaseAgent→LLMClient→agent_runs chain end-to-end for the simplest agent
(Haiku, stateless, text-in/text-out). Sets up tests/test_agents.py which plans 03 and 04 extend.

Output: pkm/agents/reader_agent.py, tests/test_agents.py (reader test + stubs), golden fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/RohitGupta/Library/Mobile Documents/com~apple~CloudDocs/Personal Knowledge Management/.planning/PROJECT.md
@/Users/RohitGupta/Library/Mobile Documents/com~apple~CloudDocs/Personal Knowledge Management/.planning/ROADMAP.md

<interfaces>
From pkm/agents/base.py (created in 02-01):
  class BaseAgent(ABC):
      role: ClassVar[str]
      model: ClassVar[str]
      prompt_template: ClassVar[str]
      prompt_version: ClassVar[str]
      input_schema: ClassVar[type]
      output_schema: ClassVar[type[BaseModel]]
      memory_tier: ClassVar[str]

      def _load_prompt(self) -> str: ...
      def run(self, llm_client, input_text: str, source_id: str | None = None,
              extra_messages: list[dict] | None = None) -> BaseModel: ...

From pkm/llm/client.py — LLMClient.__init__:
  def __init__(self, conn, api_key: str) -> None

From tests/conftest.py:
  @pytest.fixture(scope="function")
  def db_conn() -> libsql connection (auto-migrated temp DB)
  # Settings: Settings(anthropic_api_key="test-key", db_path=...)

From tests/fixtures/sample_raw.md (the golden input):
  ---
  id: src_abc123deadbee
  type: Article
  title: The Art of Taking Smart Notes
  author: Sonke Ahrens
  ---
  [2 paragraphs about smart notes / slip-box method]

ReaderAgent output schema: the Reader does NOT return a pydantic schema — it returns cleaned
Markdown text (plain string). output_schema = None for ReaderAgent.
LLMClient.call() returns {"result": str} when output_schema is None.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReaderAgent implementation</name>
  <files>pkm/agents/reader_agent.py</files>
  <read_first>
    - /Users/RohitGupta/code/pkm-engine/pkm/agents/base.py (BaseAgent ABC — read after 02-01 creates it)
    - /Users/RohitGupta/code/pkm-engine/pkm/llm/models.py (HAIKU constant)
    - /Users/RohitGupta/Library/Mobile Documents/com~apple~CloudDocs/Personal Knowledge Management/PKM_TECHNICAL_SPECIFICATION.md (Section 6 agent table — Reader row)
  </read_first>
  <behavior>
    - ReaderAgent.role == "reader_agent"
    - ReaderAgent.model == HAIKU (the string "claude-haiku-4-5-20251001")
    - ReaderAgent.output_schema is None (Reader returns cleaned text, not a pydantic model)
    - ReaderAgent.memory_tier == "stateless"
    - Instantiating ReaderAgent() does not raise
    - ReaderAgent inherits run() from BaseAgent without overriding it
  </behavior>
  <action>
    Create pkm/agents/reader_agent.py. Import BaseAgent from pkm.agents.base and HAIKU from
    pkm.llm.models.

    Define class ReaderAgent(BaseAgent) with these ClassVar attributes:
      role = "reader_agent"
      model = HAIKU
      prompt_template = "reader.v1.md"
      prompt_version = "v1"
      input_schema = None
      output_schema = None
      memory_tier = "stateless"

    Also create pkm/prompts/reader.v1.md with this content:
      ROLE: You are a Reader agent. You normalize raw captured text into clean Markdown.
      TASK: Given the raw source text below, return clean Markdown with a YAML front matter block
        containing: id, type, title, author, url, date_published, date_saved, content_hash, tags.
        Preserve the body content verbatim — do not summarize or alter the substance. Remove HTML
        artifacts, fix broken encoding, normalize whitespace.
      CONSTRAINTS: front matter must be valid YAML; body starts after the closing ---; never invent
        metadata not present in the source; if a field is unknown, omit it from front matter.
      OUTPUT: Return only the clean Markdown document. No JSON, no commentary.

    NOTE: Because output_schema = None, BaseAgent.run() returns a plain string (the cleaned
    Markdown text). The __init_subclass__ enforcement in BaseAgent checks for the ClassVar
    attributes listed above — make sure output_schema and input_schema are explicitly set to None
    (not omitted) so the hasattr check passes.
  </action>
  <verify>
    <automated>cd /Users/RohitGupta/code/pkm-engine && python -c "
from pkm.agents.reader_agent import ReaderAgent
from pkm.llm.models import HAIKU
a = ReaderAgent()
assert a.role == 'reader_agent'
assert a.model == HAIKU
assert a.output_schema is None
print('ReaderAgent OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - pkm/agents/reader_agent.py contains "class ReaderAgent(BaseAgent)"
    - pkm/agents/reader_agent.py imports HAIKU from pkm.llm.models
    - pkm/prompts/reader.v1.md exists and contains "YAML front matter"
    - python -c "from pkm.agents.reader_agent import ReaderAgent" exits 0
    - ReaderAgent().model == "claude-haiku-4-5-20251001" (verified inline)
  </acceptance_criteria>
  <done>ReaderAgent subclasses BaseAgent with role="reader_agent", model=HAIKU, output_schema=None; reader.v1.md prompt exists</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Golden-fixture test for ReaderAgent + agent_runs write</name>
  <files>tests/test_agents.py, tests/fixtures/golden_reader_output.md</files>
  <read_first>
    - /Users/RohitGupta/code/pkm-engine/tests/conftest.py (db_conn fixture shape)
    - /Users/RohitGupta/code/pkm-engine/tests/fixtures/sample_raw.md (golden input)
    - /Users/RohitGupta/code/pkm-engine/pkm/llm/client.py (LLMClient.call return shape; _write_run logic)
  </read_first>
  <behavior>
    - test_reader_agent_golden: with a mock LLMClient that returns cleaned Markdown, ReaderAgent.run() returns a non-empty string
    - test_reader_agent_golden: after run(), agent_runs table contains exactly 1 row with agent="reader_agent" and status="ok"
    - test_reader_agent_golden: the returned string contains "---" (front matter delimiter) somewhere
    - test_reader_agent_golden does NOT call the real Anthropic API (uses a stub/mock)
    - test_reader_agent_agent_runs_write: verifies tokens_in and tokens_out > 0 in agent_runs row written by a mock call
  </behavior>
  <action>
    Create tests/fixtures/golden_reader_output.md — a minimal golden output for the reader test:
      A valid Markdown file with YAML front matter containing id, type, title, author fields
      matching the sample_raw.md fixture values (id: src_abc123deadbee, type: Article,
      title: The Art of Taking Smart Notes, author: Sonke Ahrens), followed by the two-paragraph
      body from sample_raw.md verbatim.

    Create tests/test_agents.py. Structure:

    Imports: pytest, unittest.mock (MagicMock, patch), pathlib.Path, conftest's db_conn fixture
    (imported automatically by pytest), and the agent classes.

    Helper: build_mock_llm_client(conn, result_text_or_model, tokens_in=10, tokens_out=20)
      Returns a MagicMock that mimics LLMClient. The mock's .call() method returns:
        {"cached": False, "input_hash": "hash123", "result": result_text_or_model,
         "tokens_in": tokens_in, "tokens_out": tokens_out}
      AND writes a real agent_runs row to conn using the same INSERT OR REPLACE that LLMClient
      uses — so downstream assertions on agent_runs work correctly. Pass conn to the helper so
      it can write the row. This simulates LLMClient behavior without hitting the API.

    Write the row inside the mock like this (in the helper's inner function):
      import uuid, datetime
      run_id = "run_" + uuid.uuid4().hex[:20]
      now = datetime.datetime.now(datetime.UTC).isoformat().replace("+00:00","Z")
      conn.execute(
        "INSERT OR REPLACE INTO agent_runs (id, agent, source_id, input_hash, model, "
        "tokens_in, tokens_out, cost_usd, status, error, started_at, finished_at) "
        "VALUES (?,?,?,?,?,?,?,?,?,?,?,?)",
        (run_id, agent_name, None, "hash123", "mock_model",
         tokens_in, tokens_out, 0.0, "ok", None, now, now)
      )
      conn.commit()

    The mock's call() should accept **kwargs and extract agent_name from kwargs to pass to the
    row writer.

    Class TestReaderAgent:
      def test_reader_agent_golden(self, db_conn):
        Load golden_reader_output.md text as the mock result.
        Build mock llm_client.
        agent = ReaderAgent()
        result = agent.run(mock_llm_client, input_text=Path("tests/fixtures/sample_raw.md").read_text())
        Assert result is a str.
        Assert "---" in result.
        Row = db_conn.execute("SELECT status FROM agent_runs WHERE agent='reader_agent'").fetchone()
        Assert row is not None.
        Assert row[0] == "ok"

      def test_reader_agent_agent_runs_write(self, db_conn):
        Build mock with tokens_in=42, tokens_out=17.
        agent.run(mock, input_text="some text")
        Row = db_conn.execute("SELECT tokens_in, tokens_out FROM agent_runs WHERE agent='reader_agent'").fetchone()
        Assert row[0] == 42 and row[1] == 17

      def test_reader_agent_cache_hit_raises(self, db_conn):
        Build a mock whose .call() returns {"cached": True, "input_hash": "x"}.
        agent = ReaderAgent()
        With pytest.raises(RuntimeError):
          agent.run(mock, input_text="some text")

    Add placeholder test classes for future plans (so the file is a complete module):
      class TestSummarizerAgent: pass
      class TestConceptExtractor: pass
      class TestKGAgent: pass
  </action>
  <verify>
    <automated>cd /Users/RohitGupta/code/pkm-engine && python -m pytest tests/test_agents.py::TestReaderAgent -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_agents.py exists and contains "class TestReaderAgent"
    - tests/fixtures/golden_reader_output.md exists and contains "---" and "src_abc123deadbee"
    - pytest tests/test_agents.py::TestReaderAgent exits 0
    - pytest output shows "3 passed" for TestReaderAgent
    - tests/test_agents.py contains "TestSummarizerAgent", "TestConceptExtractor", "TestKGAgent" placeholder classes
  </acceptance_criteria>
  <done>Golden-fixture tests for ReaderAgent pass; agent_runs write verified via mock; placeholder test classes ready for plans 03 and 04</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test mock → agent_runs table | Mock writes real DB rows; no untrusted input |
| ReaderAgent prompt → LLM | Prompt loaded from disk; input_text is developer-controlled fixture at test time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | golden_reader_output.md | accept | Fixture is synthetic sample data; no real PII |
| T-02-04 | Denial of Service | mock LLM in tests | accept | Tests use mocks; no real API budget consumed |
| T-02-SC | Tampering | pip installs | accept | No new packages; pytest + unittest.mock are stdlib/already installed |
</threat_model>

<verification>
cd /Users/RohitGupta/code/pkm-engine && python -m pytest tests/test_agents.py::TestReaderAgent -v
# Expected: 2 passed, 0 failed
</verification>

<success_criteria>
- ReaderAgent subclasses BaseAgent; model=HAIKU; output_schema=None
- pkm/prompts/reader.v1.md exists with front matter normalization instructions
- pytest tests/test_agents.py::TestReaderAgent exits 0 with 3 passed
- agent_runs row written by mock has agent="reader_agent", status="ok", tokens_in=42, tokens_out=17
- Placeholder test classes for Summarizer, Extractor, KG exist in test_agents.py
</success_criteria>

<output>
Create /Users/RohitGupta/Library/Mobile Documents/com~apple~CloudDocs/Personal Knowledge Management/.planning/phases/02-core-agents/02-02-SUMMARY.md when done.
</output>
