---
phase: 08-hardening-mvp-gate
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - docs/PHASE8_VERIFICATION.md
  - .planning/phases/08-hardening-mvp-gate/08-MVP-REVIEW.md
autonomous: false
requirements: [MVP-01, MVP-02, MVP-04]

must_haves:
  truths:
    - "A freshly clipped article produces a synthesized wiki page with ^cite citations committed to pkm-vault (MVP-01)"
    - "The clip→wiki commit completes within the ~5 min budget; Mac state is irrelevant because no local daemon is in the path (MVP-01)"
    - "Re-clipping the same article is a complete no-op: 0 new Turso rows, 0 new LLM agent_runs rows, no new vault commit (MVP-02)"
    - "curl $QUERY_WORKER_URL/query?q=... returns a JSON {answer, citations[]} with a cited answer; no local server process is running (MVP-04)"
    - "A human reviews the MVP-01..06 evidence + Tier-1 dispositions + accepted limitations and makes the MVP-ready / V1-advancement judgment call (Type-1, Mode C)"
  artifacts:
    - path: "docs/PHASE8_VERIFICATION.md"
      provides: "MVP-01, MVP-02, MVP-04 live evidence sections appended to the doc started in Plan 01"
      contains: "MVP-01"
    - path: ".planning/phases/08-hardening-mvp-gate/08-MVP-REVIEW.md"
      provides: "Mode C MVP-review brief for the human: per-criterion PASS/FAIL, Tier-1 dispositions, accepted limitations, advancement-gate status"
      contains: "MVP Review"
  key_links:
    - from: "clip (POST worker-clip /clip)"
      to: "pkm-vault raw/ commit + repository_dispatch"
      via: "Cloudflare Worker → GitHub Contents API → Actions ingest.yml"
      pattern: "repository_dispatch|raw/"
    - from: "Actions ingest.yml"
      to: "pkm-vault wiki/ commit"
      via: "run_ingest + GitVaultWriter bot commit"
      pattern: "wiki/"
    - from: "curl $QUERY_WORKER_URL/query"
      to: "{answer, citations}"
      via: "Workers AI embed → Vectorize search → Turso fetch → OpenAI synthesis"
      pattern: "answer.*citations"
---

<objective>
Run the live end-to-end MVP demo (MVP-01 clip→wiki, MVP-02 re-clip no-op, MVP-04 edge query) and surface the Mode C MVP-review brief for a human judgment call.

Purpose: MVP-01, MVP-02, MVP-04 are the acceptance criteria that can only be proven against the live cloud path (clip worker → GitHub Actions → Turso/OpenAI → vault → query worker). MVP-01 ("Mac can be asleep") is satisfied architecturally — no local daemon is in the path — and is demonstrated by running the whole flow from CLI curl without any local server. This plan also produces the MVP-review brief that consolidates all six criteria (Plans 01+02 evidence plus this plan's live evidence) and the Tier-1 dispositions, then pauses for the human to make the Type-1 MVP-ready / V1-advancement call. The project hard rule (CLAUDE.md): "Stop at MVP gate — do NOT start V1 autonomously after Phase 8." This plan does NOT begin V1 work.

Output: docs/PHASE8_VERIFICATION.md with MVP-01/MVP-02/MVP-04 live evidence; .planning/phases/08-hardening-mvp-gate/08-MVP-REVIEW.md Mode C brief; a checkpoint:human-verify task for the MVP-ready judgment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@PROGRESS.md
@DECISIONS.md
@docs/GUARDRAILS.md
@docs/PHASE8_VERIFICATION.md
@.planning/phases/08-hardening-mvp-gate/08-01-SUMMARY.md
@.planning/phases/08-hardening-mvp-gate/08-02-SUMMARY.md

<interfaces>
<!-- Live endpoints (values are operator-local secrets/env — do NOT paste into committed docs) -->
- Clip worker: POST https://<clip-worker>.workers.dev/clip  with header X-PKM-Key: <PKM_KEY> and JSON body {url, type, text, title}
    (worker-clip.js deployed in Phase 5; key at ~/.pkm_key per PROGRESS.md Phase 6)
- Query worker: GET https://pkm-query.rohitgupta-iitr.workers.dev/query?q=<question>  with header X-PKM-Key: <PKM_KEY>
    (deployed Phase 6 Wave 3; returns {answer, citations:[{claim_id, statement, source_title, raw_path, url}]})
- GitHub Actions: ingest.yml triggers on repository_dispatch (type: ingest) fired by the clip worker
    `gh run list --workflow=ingest.yml --limit 3` and `gh run watch <id>` to observe the run
- Turso: TURSO_URL / TURSO_TOKEN in .env — query via pkm.store.registry.connect(Settings()) for
    SELECT COUNT(*) FROM sources / chunks / claims / agent_runs  (before/after re-clip)
- Vault: pkm-vault HEAD sha via `gh api repos/rohitgupta1686/pkm-vault/commits/main --jq .sha`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live end-to-end demo evidence (MVP-01, MVP-02, MVP-04)</name>
  <files>docs/PHASE8_VERIFICATION.md</files>
  <read_first>
    - docs/PHASE8_VERIFICATION.md (the MVP-05/MVP-03 sections written by Plan 01 — append MVP-01/MVP-02/MVP-04 sections to the same doc)
    - docs/GUARDRAILS.md "Verification" section (the Phase-7 live-run evidence format — mirror its table style)
    - PROGRESS.md "Phase 6 Wave 3 COMPLETE" section (the exact live query-worker URL + the curl shape used: `curl "$QUERY_WORKER_URL/query?q=..." -H "X-PKM-Key: $(cat ~/.pkm_key)"`)
    - PROGRESS.md "Phase 5 DoD Evidence" (clip-worker POST shape + X-PKM-Key auth)
    - .planning/phases/08-hardening-mvp-gate/08-01-SUMMARY.md and 08-02-SUMMARY.md (the already-captured MVP-05/MVP-03/MVP-06 evidence — the brief in Task 2 consolidates these)
    - pkm/batch.py _running_totals (the SQL to confirm 0 new agent_runs rows on re-clip)
  </read_first>
  <action>
    Run the live demo and append three evidence sections to docs/PHASE8_VERIFICATION.md. Use the operator-local environment for secrets (PKM_KEY from ~/.pkm_key, TURSO_URL/TURSO_TOKEN/VAULT_PATH from .env, worker URLs from PROGRESS.md Phase 6). If any required secret is not present in the local env, stop and create a checkpoint:human-action to obtain it (do NOT ask for the secret value to be pasted into the repo).

    MVP-01 — clip → synthesized wiki page within ~5 min, Mac asleep:
    1. Pick a fresh test article URL not already in raw/ (or synthesize a small unique text body so the content_hash is new).
    2. Record T0 timestamp. POST to the clip worker /clip with X-PKM-Key and the article body.
    3. Watch the triggered Actions run: `gh run list --workflow=ingest.yml --limit 1` then `gh run watch <id>`. Record T1 (run conclusion timestamp). Compute T1-T0; confirm <= ~5 min (the workflow timeout-minutes is 10; the criterion is ~5 min).
    4. Confirm a new synthesized wiki page with ^cite citations was committed to pkm-vault: `gh api repos/rohitgupta1686/pkm-vault/commits/main --jq '.sha'` and inspect the commit's added files under wiki/sources/ for ^cite: anchors.
    5. Confirm "Mac can be asleep" is satisfied architecturally: no local server process was started; the entire path is clip worker → GitHub Actions → Turso/OpenAI → vault. Record that `pgrep -f 'pkm|uvicorn|fastapi'` (or equivalent) shows no local daemon running.

    MVP-02 — re-clip same article = complete no-op:
    1. Before re-clip, capture baseline: SELECT COUNT(*) FROM sources, chunks, claims, agent_runs (via pkm.store.registry.connect); capture the vault HEAD sha.
    2. Re-clip the SAME article (same body → same content_hash). The clip worker may still fire repository_dispatch (T2-05-03); the pipeline must dedup.
    3. After the run completes, re-query the same four COUNT(*) values and the vault HEAD sha.
    4. Confirm: 0 new sources rows, 0 new chunks, 0 new claims, 0 new agent_runs rows (the LLM hash cache → 0 LLM calls), and no new vault commit (HEAD sha unchanged). Record the before/after counts verbatim.

    MVP-04 — query worker returns cited answer, no local server:
    1. Confirm no local server is running (same pgrep check).
    2. `curl -s "$QUERY_WORKER_URL/query?q=<question>" -H "X-PKM-Key: $(cat ~/.pkm_key)"` with a question answerable from the corpus.
    3. Confirm the JSON response has a non-empty `answer` string and a `citations` array with at least one entry whose `raw_path` exists in pkm-vault (`gh api repos/.../contents/<raw_path>` returns 200).
    4. Record the answer text (or first 200 chars) and the citations count + one raw_path. Do NOT record the X-PKM-Key value.

    Each section gets a PASS/FAIL verdict and the concrete evidence above. No secret values in the doc.
  </action>
  <verify>
    <automated>cd /Users/RohitGupta/code/pkm-engine && grep -c "MVP-01" docs/PHASE8_VERIFICATION.md && grep -c "MVP-02" docs/PHASE8_VERIFICATION.md && grep -c "MVP-04" docs/PHASE8_VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/PHASE8_VERIFICATION.md contains MVP-01, MVP-02, MVP-04 sections (`grep -c "MVP-0[124]"` each >= 1)
    - MVP-01 section records T0, T1, the T1-T0 duration <= ~5 min, the new vault commit sha, and confirms a wiki/sources/ page with ^cite: anchors was added
    - MVP-01 section records that no local daemon/server process was running (the "Mac asleep" architectural satisfaction)
    - MVP-02 section records before/after COUNT(*) for sources/chunks/claims/agent_runs showing 0 new rows in each, AND the unchanged vault HEAD sha, AND 0 new agent_runs rows (= 0 LLM calls)
    - MVP-04 section records a non-empty `answer` and a `citations` array with >= 1 entry, plus a raw_path verified to exist in pkm-vault, and confirms no local server process
    - Each of the three sections has an explicit PASS/FAIL verdict
    - `grep -iE "X-PKM-Key: [^$]|sk-|TURSO_TOKEN=|OPENAI_API_KEY=|x-access-token:[A-Za-z0-9]" docs/PHASE8_VERIFICATION.md` returns nothing (no secret values; the `$(cat ~/.pkm_key)` substitution form is fine because it is not the literal key)
  </acceptance_criteria>
  <done>docs/PHASE8_VERIFICATION.md has MVP-01 (clip→wiki<=~5min, no local daemon), MVP-02 (0 new rows + 0 LLM calls + no new commit), MVP-04 (cited answer, no local server) sections, each with a PASS/FAIL verdict and no secret material.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Surface Mode C MVP-review brief — human MVP-ready judgment (Type-1)</name>
  <action>
Before pausing, write .planning/phases/08-hardening-mvp-gate/08-MVP-REVIEW.md — the Mode C MVP-review brief. Consolidate the six MVP-criterion PASS/FAIL verdicts: MVP-01/MVP-02/MVP-04 from docs/PHASE8_VERIFICATION.md (Plan 03 Task 1), MVP-05/MVP-03 from docs/PHASE8_VERIFICATION.md (Plan 01), MVP-06 from PROGRESS.md (Plan 02). Include a per-criterion table with evidence citations (file + section). List the Tier-1 dispositions from DECISIONS.md "Phase 8 MVP-gate review" (T1-01 Vectorize reaffirm; T1-02 OpenAI three-condition verification). List accepted limitations carried to V1: the missing-provenance best-effort limitation (DECISIONS.md T2-05-04, drop-FK/free-text-provenance deferred as a Type-1 contract change) and the dashboard counter backfill (now closed by Plan 01). Explicitly state that V1+ advancement is NOT authorized at this gate and reference the ROADMAP "Advancement Gates (Post-MVP)" triggers (corpus >=150 sources, long-context misses, relational question recurrence) — none are met at MVP. Record no secret values. Do NOT declare MVP-ready in the brief — present the evidence and the three decision options (MVP-ready / MVP-ready with limitations / NOT-ready) for the human to choose at the checkpoint.
  </action>
  <what-built>
    Phase 8 produced: (a) Plan 01 — full test suite green (MVP-05) + lint provenance evidence (MVP-03) + dashboard counter backfill; (b) Plan 02 — cost actuals recorded in PROGRESS.md (MVP-06) + Tier-1 batch (T1-01, T1-02) reviewed in DECISIONS.md; (c) Plan 03 Task 1 — live clip→wiki (MVP-01), re-clip no-op (MVP-02), edge query (MVP-04) evidence. Claude has gathered all six MVP-criterion evidence items but has NOT declared MVP-ready — that is a human judgment call (Type-1, irreversible per CLAUDE.md "Stop at MVP gate").
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/08-hardening-mvp-gate/08-MVP-REVIEW.md` (Claude writes this brief before pausing — it consolidates the six PASS/FAIL verdicts from docs/PHASE8_VERIFICATION.md + PROGRESS.md + DECISIONS.md, the Tier-1 dispositions, and the accepted limitations).
    2. Verify each MVP-0X criterion against the cited evidence:
       - MVP-01: clip→wiki within ~5 min, no local daemon — docs/PHASE8_VERIFICATION.md MVP-01 section
       - MVP-02: re-clip = 0 new rows, 0 LLM calls, no new commit — docs/PHASE8_VERIFICATION.md MVP-02 section
       - MVP-03: every wiki claim resolves to a raw/ span — docs/PHASE8_VERIFICATION.md MVP-03 section (note the accepted best-effort-provenance limitation from DECISIONS.md T2-05-04: missing-provenance count carried as accepted-MVP limitation; drop-FK/free-text-provenance deferred to V1 as a Type-1 contract change)
       - MVP-04: query worker returns cited answer, no local server — docs/PHASE8_VERIFICATION.md MVP-04 section
       - MVP-05: full test suite green — docs/PHASE8_VERIFICATION.md MVP-05 section
       - MVP-06: cost actuals infra $0 + OpenAI $/mo <= cap — PROGRESS.md Cost Actuals table
    3. Review the Tier-1 batch dispositions in DECISIONS.md "Phase 8 MVP-gate review" (T1-01 Vectorize reaffirm; T1-02 OpenAI three-condition verification).
    4. Decide ONE of: (a) MVP-ready — accept the system as MVP, hold at the gate; (b) MVP-ready with documented limitations — accept and log the limitations (missing-provenance best-effort, any others) for V1; (c) NOT-ready — specify which criterion failed and the required remediation (a Phase 8 gap-closure round).
    5. Do NOT authorize V1 work (Chroma, 12 templates, hybrid retrieval, Neo4j, GraphRAG, V2/V3 agents) in this checkpoint. V1+ advancement is a separate human decision per ROADMAP "Advancement Gates (Post-MVP)" — advancement triggers (corpus >=150 sources, long-context misses, relational question recurrence) are NOT met at MVP. This checkpoint is only the MVP-ready judgment.
  </how-to-verify>
  <resume-signal>Type "MVP-ready", "MVP-ready with limitations: <list>", or "NOT-ready: <criterion + remediation>". Do NOT type "start V1".</resume-signal>
  <acceptance_criteria>
    - `.planning/phases/08-hardening-mvp-gate/08-MVP-REVIEW.md` exists and contains a per-criterion PASS/FAIL table for MVP-01..06 with evidence citations
    - The brief lists the Tier-1 dispositions (T1-01, T1-02) and the accepted limitations (missing-provenance best-effort per T2-05-04, and any others surfaced)
    - The brief explicitly states V1 advancement is NOT authorized at this gate and references the ROADMAP advancement triggers
    - The human's resume signal is one of the three specified forms (MVP-ready / MVP-ready with limitations / NOT-ready)
    - No V1/V2/V3 work was begun in Phase 8 (`git diff main -- pkm/ | grep -iE "chroma|neo4j|graphrag|hybrid|leiden"` returns nothing)
  </acceptance_criteria>
  <done>Human has issued an MVP-ready judgment (one of the three forms); 08-MVP-REVIEW.md records the verdict and accepted limitations; no V1 work started.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator local env → clip worker | POST /clip crosses the trust boundary with X-PKM-Key shared secret |
| operator local env → query worker | GET /query crosses with X-PKM-Key |
| clip worker → GitHub | worker uses VAULT_PAT (contents:write on pkm-vault) + GH_PAT (dispatch on pkm-engine) |
| GitHub Actions → Turso / OpenAI | ingest workflow uses TURSO_TOKEN + OPENAI_API_KEY |
| verification doc + MVP-review brief → public repo | both committed to public pkm-engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Information disclosure | docs/PHASE8_VERIFICATION.md, 08-MVP-REVIEW.md | mitigate | Record only commit shas, counts, durations, answer excerpts, raw_paths. Acceptance criterion greps for literal secret values (X-PKM-Key: <literal>, sk-, TURSO_TOKEN=, x-access-token:<literal>) and must return nothing. The `$(cat ~/.pkm_key)` substitution form is allowed (not the literal key). |
| T-08-08 | Spoofing | clip / query worker X-PKM-Key | accept | Shared-secret auth already verified in Phase 5 (timingSafeEqual). Demo re-uses the existing key; no new key material is introduced. |
| T-08-09 | Repudiation | live demo evidence | mitigate | Record concrete artifacts: Actions run URL (gh run list output), vault commit sha, before/after Turso COUNT(*) values, curl response excerpt — all re-verifiable by a third party. |
| T-08-10 | Elevation of privilege | MVP-ready declaration | mitigate | The MVP-ready / V1-advancement decision is a Type-1 (irreversible) human judgment. Claude gathers evidence only and is explicitly forbidden from declaring MVP-ready or starting V1. The checkpoint:human-verify gate enforces the human sign-off; acceptance criteria forbid "start V1" as a resume signal. |
| T-08-11 | Denial of service / cost | live clip + ingest | mitigate | The demo triggers one real OpenAI ingest run (bounded spend). Per-run cap PKM_RUN_COST_CAP_USD (default 0.50) enforces abort; OpenAI monthly hard limit (GUARD-05) bounds month-total. One clip + one re-clip (no-op) is well within bounds. |
| T-08-SC | Tampering | npm/pip installs | accept | No package installs in this plan — pure curl/gh/SQL evidence gathering. No supply-chain surface. |
</threat_model>

<verification>
- MVP-01: clip→wiki commit within ~5 min, no local daemon (docs/PHASE8_VERIFICATION.md)
- MVP-02: re-clip → 0 new sources/chunks/claims/agent_runs rows + unchanged vault sha (docs/PHASE8_VERIFICATION.md)
- MVP-04: curl query worker → {answer, citations[]} with raw_path existing in vault, no local server (docs/PHASE8_VERIFICATION.md)
- 08-MVP-REVIEW.md consolidates MVP-01..06 verdicts + Tier-1 dispositions + accepted limitations
- Human issues MVP-ready judgment via the checkpoint; no V1 work started
</verification>

<success_criteria>
- MVP-01, MVP-02, MVP-04 live evidence captured in docs/PHASE8_VERIFICATION.md with PASS/FAIL verdicts and no secrets
- 08-MVP-REVIEW.md presents the consolidated six-criterion brief + Tier-1 dispositions + accepted limitations
- Human makes the Type-1 MVP-ready judgment (autonomous:false checkpoint satisfied)
- No V1/V2/V3 work begun (CLAUDE.md "Stop at MVP gate" honored)
</success_criteria>

<output>
Create `.planning/phases/08-hardening-mvp-gate/08-03-SUMMARY.md` when done (record the human's MVP-ready verdict and any accepted limitations)
</output>