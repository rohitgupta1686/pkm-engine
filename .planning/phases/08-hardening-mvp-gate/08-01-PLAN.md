---
phase: 08-hardening-mvp-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkm/store/registry.py
  - pkm/cli.py
  - tests/test_dashboard.py
  - docs/PHASE8_VERIFICATION.md
autonomous: true
requirements: [MVP-05, MVP-03]

must_haves:
  truths:
    - "Full pytest suite passes with zero failures (MVP-05)"
    - "npm clip-worker and query-worker test suites pass with zero failures (MVP-05)"
    - "pkm lint reports missing-provenance and broken-wikilink counts, captured as MVP-03 evidence"
    - "dashboard_counters rows reflect historical totals (Sources/Claims/Concepts no longer read 0) after the one-time backfill"
  artifacts:
    - path: "pkm/store/registry.py"
      provides: "seed_counters_from_live_counts(conn) — one-time backfill of dashboard_counters from live table counts"
      contains: "def seed_counters_from_live_counts"
    - path: "pkm/cli.py"
      provides: "pkm backfill-counters subcommand"
      contains: "backfill-counters"
    - path: "tests/test_dashboard.py"
      provides: "test asserting seed_counters_from_live_counts sets counters from live counts and is idempotent"
      contains: "seed_counters_from_live_counts"
    - path: "docs/PHASE8_VERIFICATION.md"
      provides: "MVP-05 test-suite evidence + MVP-03 provenance evidence sections"
      contains: "MVP-05"
  key_links:
    - from: "pkm/cli.py"
      to: "pkm/store/registry.py::seed_counters_from_live_counts"
      via: "backfill-counters subcommand handler"
      pattern: "seed_counters_from_live_counts"
---

<objective>
Automated hardening + verification: close the Phase-7 dashboard-counter backfill follow-up, run the full test suite green (MVP-05), and capture provenance/lint evidence for MVP-03.

Purpose: MVP-05 (full test suite green) and MVP-03 (every wiki claim resolves to a raw/ source span) are the two acceptance criteria that can be proven by automated commands alone. The dashboard counter backfill is a known Phase-7 carry-in (STATE.md "Known follow-up") that would otherwise leave dashboard.md showing 0 sources/claims/concepts at the MVP gate. This plan closes that gap and records the automated evidence.

Output: `seed_counters_from_live_counts` helper + `pkm backfill-counters` CLI + test; `docs/PHASE8_VERIFICATION.md` with MVP-05 and MVP-03 evidence sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@docs/GUARDRAILS.md
@PROGRESS.md

<interfaces>
<!-- Existing counter helpers the backfill must reuse. From pkm/store/registry.py -->
- bump_counter(conn, key: str, delta: int = 1, commit: bool = True) -> int   # line 54
- read_counter(conn, key: str) -> int                                        # line 76
- read_all_counters(conn) -> dict[str, int]                                  # line 85
- dashboard_counters table (key TEXT PK, value INT, updated_at) per migrations/sqlite/003_dashboard_counters.sql
- Counter keys already bumped on insert: sources, concepts, claims (see upsert_source / upsert_concept / insert_claim wiring, Plan 07-02)

<!-- CLI pattern: argparse subparsers in pkm/cli.py. _build_parser() adds subparsers;
     app() dispatches on args.subcommand string. Existing subcommands: ingest,
     batch-ingest, lint, dashboard, backfill-embeds. New one mirrors backfill-embeds. -->

<!-- Lint report shape (pkm/lint.py, Plan 07-01): lint_vault(conn, vault_root, write_log, now)
     returns LintReport with broken/orphan/missing_provenance counts. CLI prints
     "lint ok" or "lint FAIL broken=N orphan=N missing_provenance=N". -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One-time dashboard counter backfill (closes Phase-7 known follow-up)</name>
  <files>pkm/store/registry.py, pkm/cli.py, tests/test_dashboard.py</files>
  <read_first>
    - pkm/store/registry.py (current bump_counter/read_counter/read_all_counters + the dashboard counter keys bumped in upsert_source/upsert_concept/insert_claim — match the exact key strings)
    - pkm/cli.py (the backfill-embeds subcommand block at lines ~131-152 and its _cmd_backfill_embeds handler — mirror that structure for backfill-counters; app() dispatch at lines ~156-177)
    - migrations/sqlite/003_dashboard_counters.sql (dashboard_counters schema: key, value, updated_at)
    - tests/test_dashboard.py (existing counter tests — match their fixture/conn style)
    - .planning/STATE.md "Known follow-up" paragraph (why this backfill is needed)
  </read_first>
  <behavior>
    - seed_counters_from_live_counts(conn) sets dashboard_counters row "sources" = SELECT COUNT(*) FROM sources
    - sets "claims" = SELECT COUNT(*) FROM claims
    - sets "concepts" = SELECT COUNT(*) FROM concepts
    - sets any other counter keys already used by bump_counter (match the exact keys from upsert_*/insert_claim wiring) to their live table counts
    - is idempotent: running twice yields the same counter values (uses the same upsert/INSERT OR REPLACE pattern as bump_counter, not incrementing)
    - does NOT use bump_counter (which increments); it writes absolute counts
    - commits the transaction
    - returns a dict {key: seeded_value} for the CLI to print
  </behavior>
  <action>
    Add `seed_counters_from_live_counts(conn, commit=True) -> dict[str,int]` to pkm/store/registry.py. Read the exact counter key strings currently bumped by upsert_source, upsert_concept, and insert_claim (grep for bump_counter call sites) and seed exactly those keys from their corresponding live-table COUNT(*) (sources, concepts, claims). Use INSERT OR REPLACE into dashboard_counters (absolute value, not increment) keyed by each counter key, with updated_at set to now. Parameterized ? placeholders throughout (T-02-08 SQL-injection mitigation). Do not introduce a new migration — the table already exists from 003.

    Add a `backfill-counters` subcommand to pkm/cli.py mirroring the `backfill-embeds` subcommand: subparser with a `--vault` arg (default settings.vault_path), a `_cmd_backfill_counters(args)` handler that connects via pkm.store.registry.connect(Settings()), calls seed_counters_from_live_counts(conn), and prints the resulting {key: value} dict only (never settings or secrets). Add the dispatch branch `elif args.subcommand == "backfill-counters": _cmd_backfill_counters(args)` in app(). No new dependency imports at module top — late-import inside the handler like the other commands.

    Add tests to tests/test_dashboard.py asserting: (1) after seeding, read_all_counters returns sources/claims/concepts matching live COUNT(*); (2) seeding twice yields identical values (idempotent); (3) seeding on an empty DB yields 0 for each (no crash). Match the existing test file's fixture/conn style.

    Then run the backfill once against the live Turso DB: `pkm backfill-counters` (with .env holding TURSO_URL/TURSO_TOKEN/VAULT_PATH). Capture the printed dict for the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/RohitGupta/code/pkm-engine && python -m pytest tests/test_dashboard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def seed_counters_from_live_counts" pkm/store/registry.py` returns 1
    - `grep -c "backfill-counters" pkm/cli.py` returns at least 2 (subparser name + dispatch branch)
    - `python -m pytest tests/test_dashboard.py -q` passes with 0 failures (new tests included)
    - `python -c "from pkm.store.registry import seed_counters_from_live_counts; assert callable(seed_counters_from_live_counts)"` exits 0
    - `grep -v '^#' pkm/store/registry.py | grep -c "INSERT OR REPLACE INTO dashboard_counters"` >= 1 (absolute upsert, not increment)
    - `pkm backfill-counters` run against live Turso prints a dict with non-zero sources/claims/concepts values (records the historical ~160 claims) — paste the printed dict into the plan SUMMARY
  </acceptance_criteria>
  <done>seed_counters_from_live_counts helper + backfill-counters CLI + tests committed; live backfill run produces non-zero Sources/Claims/Concepts counters in Turso (Phase-7 known follow-up closed).</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite green + MVP-03 provenance evidence</name>
  <files>docs/PHASE8_VERIFICATION.md</files>
  <read_first>
    - docs/GUARDRAILS.md "Verification" section (the Phase-7 run-27901063045 evidence format — mirror it for Phase 8)
    - docs/PHASE5_VERIFICATION.md and docs/PHASE4_VERIFICATION.md (existing per-phase verification doc format)
    - PROGRESS.md "Phase 7 Code Complete" section (the 134-passed baseline + how prior phases recorded test counts)
    - .planning/REQUIREMENTS.md MVP-05 and MVP-03 lines (exact acceptance wording)
    - pkm/lint.py (the lint report fields: broken, orphan, missing_provenance — these are the MVP-03 evidence numbers)
    - DECISIONS.md [T2-05-04] (the chunk_id best-effort / drop-FK-deferred note — needed to interpret a non-zero missing_provenance count correctly)
  </read_first>
  <action>
    Run the full automated suite and capture results into a new docs/PHASE8_VERIFICATION.md:
    1. `python -m pytest -q` from repo root — record total passed/failed/warnings (expected: 134+ passed, 0 failed; the +N from Task 1's new tests).
    2. `npm test` (clip worker vitest) — record pass count (expected: 13).
    3. `npm run test:query` (query worker) — record pass count (expected: 19).
    4. `pkm lint --no-write-log` (or `pkm lint` against the vault) — capture the `broken=N orphan=N missing_provenance=N` line. These are the MVP-03 evidence numbers.

    Write docs/PHASE8_VERIFICATION.md with two sections:
    - "## MVP-05 — Full test suite green": a table with the three commands above, their expected vs actual pass counts, and a PASS/FAIL verdict per command. Paste the actual pytest summary line (e.g. "N passed, 0 failed in Xs").
    - "## MVP-03 — Every wiki claim resolves to a raw/ source span": record the lint broken-wikilink count and missing-provenance count. Interpret against DECISIONS.md [T2-05-04]: broken-wikilinks must be 0 for MVP-03 to PASS (a broken [[wikilink]] means a claim/citation does not resolve). missing-provenance (claims with chunk_id IS NULL) is the known best-effort-provenance limitation from T2-05-04; record the count verbatim and mark MVP-03 as PASS-if-broken=0, with the missing-provenance count carried into the Plan 03 MVP-review brief as an accepted-MVP limitation (the drop-FK / free-text-provenance alternative is deferred to V1 as a Type-1 contract change per T2-05-04). Do NOT mark MVP-03 PASS if broken-wikilinks > 0 — instead surface as a blocker in the SUMMARY.

    Record only PASS/FAIL + counts + command output. Record NO secret values (per GUARDRAILS.md no-secrets rule). If `pkm lint` is not runnable locally without creds, document exactly which command was run and its stdout.
  </action>
  <verify>
    <automated>cd /Users/RohitGupta/code/pkm-engine && python -m pytest -q 2>&1 | tail -3 && test -f docs/PHASE8_VERIFICATION.md && grep -c "MVP-05" docs/PHASE8_VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/PHASE8_VERIFICATION.md exists and `grep -c "MVP-05" docs/PHASE8_VERIFICATION.md` >= 1 and `grep -c "MVP-03" docs/PHASE8_VERIFICATION.md` >= 1
    - The MVP-05 section records a concrete pytest summary line with "0 failed" (paste the literal line, e.g. "137 passed, 0 failed")
    - The MVP-05 section records npm test and npm run test:query pass counts
    - The MVP-03 section records the literal lint line `broken=N orphan=N missing_provenance=N` with real N values
    - The MVP-03 section states an explicit PASS/FAIL verdict gated on broken-wikilinks == 0
    - `grep -iE "secret|token|pat|api.key|TURSO_TOKEN|OPENAI_API_KEY|PKM_KEY" docs/PHASE8_VERIFICATION.md | grep -v "recorded as set"` returns no secret values (only references that a value is set, never the value)
  </acceptance_criteria>
  <done>docs/PHASE8_VERIFICATION.md contains MVP-05 (all three test suites green, 0 failed) and MVP-03 (lint counts + PASS/FAIL verdict) evidence sections; no secrets recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → Turso | `pkm backfill-counters` / `pkm lint` use TURSO_TOKEN to read live tables |
| verification doc → public repo | docs/PHASE8_VERIFICATION.md is committed to the public pkm-engine repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information disclosure | docs/PHASE8_VERIFICATION.md | mitigate | Record only PASS/FAIL + counts + command stdout. Never record secret values (TURSO_TOKEN, OPENAI_API_KEY, PKM_KEY, VAULT_PAT). Acceptance criterion includes a grep gate that no secret values appear. |
| T-08-02 | Tampering | dashboard_counters backfill | accept | seed_counters_from_live_counts writes absolute COUNT(*) values via INSERT OR REPLACE; idempotent; read-only source tables. A bad backfill is reversible by re-running. |
| T-08-03 | Repudiation | test-suite evidence | mitigate | Paste literal pytest/npm summary lines (e.g. "137 passed, 0 failed") rather than paraphrasing — verifiable by re-running the same commands. |
| T-08-SC | Tampering | npm/pip installs | accept | No new package installs in this plan — reuses existing pytest/vitest and existing pkm modules. No supply-chain surface added. |
</threat_model>

<verification>
- `python -m pytest -q` → 0 failed (MVP-05 automated portion)
- `npm test` and `npm run test:query` → 0 failed (MVP-05 automated portion)
- `pkm lint` → broken-wikilinks == 0 (MVP-03 hard gate); missing-provenance count recorded as accepted limitation
- `pkm backfill-counters` → non-zero Sources/Claims/Concepts counters in Turso (Phase-7 known follow-up closed)
- docs/PHASE8_VERIFICATION.md contains MVP-05 and MVP-03 sections with no secret material
</verification>

<success_criteria>
- Full pytest + npm clip + npm query suites green (MVP-05 automated evidence captured)
- MVP-03 lint evidence captured with explicit PASS/FAIL verdict (broken-wikilinks == 0)
- Dashboard counter backfill shipped + run against live Turso (Phase-7 carry-in closed)
- No V1/V2/V3 work introduced (no Chroma, no Neo4j, no new agents, no hybrid retrieval)
</success_criteria>

<output>
Create `.planning/phases/08-hardening-mvp-gate/08-01-SUMMARY.md` when done
</output>