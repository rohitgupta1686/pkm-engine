---
phase: 08-hardening-mvp-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - PROGRESS.md
  - DECISIONS.md
autonomous: true
requirements: [MVP-06]

must_haves:
  truths:
    - "PROGRESS.md Cost Actuals table is filled with real values: infra $0/mo, GitHub Actions $0, Cloudflare $0, Turso $0, OpenAI $/mo from agent_runs"
    - "OpenAI $/mo is derived from actual agent_runs.cost_usd totals, not estimated or 'TBD'"
    - "Per-run caps (PKM_RUN_COST_CAP_USD, PKM_RUN_TOKEN_CAP) and OpenAI monthly hard limit (GUARD-05) confirmed in PROGRESS.md as the cost-control evidence"
    - "DECISIONS.md Tier-1 Batch has a Phase 8 MVP-gate disposition for T1-01 (Vectorize) and T1-02 (OpenAI backend) with each condition verified"
    - "T1-02's three locked conditions are verified in DECISIONS.md: sync API dispatch, cost_usd computed from usage, cache-bust accepted"
  artifacts:
    - path: "PROGRESS.md"
      provides: "Filled Cost Actuals table with real agent_runs-derived OpenAI spend + $0 infra"
      contains: "Cost Actuals"
    - path: "DECISIONS.md"
      provides: "Phase 8 MVP-gate review section with T1-01/T1-02 dispositions + condition verification"
      contains: "Phase 8 MVP-gate"
  key_links:
    - from: "PROGRESS.md Cost Actuals table"
      to: "agent_runs.cost_usd (Turso)"
      via: "SQL SUM(cost_usd) over agent_runs"
      pattern: "SUM\\(cost_usd\\)|agent_runs"
    - from: "DECISIONS.md T1-02 condition 2 verification"
      to: "pkm/llm/pricing.py::compute_cost"
      via: "client.py line 352 compute_cost call"
      pattern: "compute_cost"
---

<objective>
Record cost actuals (MVP-06) and finalize the Tier-1 batch review in DECISIONS.md.

Purpose: MVP-06 requires cost actuals recorded in PROGRESS.md — infra $0 and real Claude/OpenAI $/mo derived from agent_runs, not estimates. This is also the moment to verify the Tier-1 decisions accumulated since Phase 1 (T1-01 Vectorize, T1-02 OpenAI backend) against their locked conditions and record the MVP-gate disposition. T1-02 condition 2 (cost_usd computed from usage, not hardcoded 0.0) is directly load-bearing for MVP-06 cost evidence — if it were still 0.0, the cost actuals would be meaningless.

Output: Filled PROGRESS.md Cost Actuals table; DECISIONS.md "Phase 8 MVP-gate review" section with T1-01/T1-02 dispositions and condition checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@PROGRESS.md
@DECISIONS.md
@docs/GUARDRAILS.md

<interfaces>
<!-- Cost aggregation helper already in the codebase. From pkm/batch.py -->
- _running_totals(conn, batch_start_iso) -> (cost_usd_total, tokens_total)
    SQL: "SELECT COALESCE(SUM(cost_usd), 0.0), COALESCE(SUM(tokens_in + tokens_out), 0) FROM agent_runs WHERE started_at >= ?"
    (pkm/batch.py lines 33-37) — reuse this pattern for a month-bucketed cost query.

<!-- Real per-call cost computation. From pkm/llm/pricing.py -->
- PRICING: dict[str, dict[str, float]]  (line 13)
- compute_cost(model, tokens_in, cached_tokens, tokens_out) -> float  (line 22)
    "KeyError on unknown model — intentional, fail loud, never return 0.0" (line 38)
    Called from pkm/llm/client.py line 352: cost_usd = compute_cost(model, tokens_in, cached_tokens, tokens_out)

<!-- Per-run caps enforced in pkm/batch.py -->
- PKM_RUN_COST_CAP_USD (default 0.50) — batch.py aborts run if cumulative cost_usd >= cap (line 117)
- PKM_RUN_TOKEN_CAP (default 200000) — batch.py aborts run if cumulative tokens >= cap

<!-- Tier-1 batch items already in DECISIONS.md -->
- [T1-01] Vectorize chosen over Turso native vectors (cloud doc §7.2)
- [T1-02] Cloud pipeline LLM backend Anthropic → OpenAI gpt-5.4-mini (locked 2026-06-19) with 3 conditions:
    1. Dispatch path = standard sync API, NOT Batch (ingest.yml timeout-minutes: 10)
    2. $0-infra exception gated on per-run token/$ ceiling + real cost_usd from usage (NOT hardcoded 0.0 — client.py:220 was the blocker)
    3. Cache-bust accepted (one-time full re-ingest; _make_input_hash includes model string)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record cost actuals in PROGRESS.md (MVP-06)</name>
  <files>PROGRESS.md</files>
  <read_first>
    - PROGRESS.md "Cost Actuals" section (lines ~23-34 — the table to fill; current values are "—" / "TBD")
    - pkm/batch.py lines 30-40 (_running_totals SQL pattern — reuse for a month-bucketed SUM(cost_usd))
    - pkm/llm/pricing.py (confirm PRICING has the gpt-5.4-mini entry and compute_cost is real, not a stub)
    - pkm/llm/client.py line ~352 (confirm cost_usd = compute_cost(...) — the T1-02 condition-2 verification)
    - docs/GUARDRAILS.md GUARD-05 section (OpenAI monthly hard limit + per-run caps already confirmed 2026-06-21)
    - .planning/REQUIREMENTS.md MVP-06 line (exact acceptance wording: "infra $0 target, Claude $/mo")
  </read_first>
  <action>
    First verify T1-02 condition 2 holds (it is load-bearing for MVP-06): confirm pkm/llm/client.py computes cost_usd via pkm.llm.pricing.compute_cost (not hardcoded 0.0). If it is still 0.0, STOP — that is a Mode-C blocker (cost actuals would be meaningless) and surface it instead of recording fake numbers.

    Then derive the actual OpenAI spend from Turso agent_runs. Use the _running_totals SQL pattern from pkm/batch.py to query cumulative cost_usd and tokens across ALL agent_runs rows (no started_at filter for the total; optionally a month-bucketed breakdown by calendar month using strftime('%Y-%m', started_at)). Run the query against the live Turso DB via `pkm` or a one-off python -c using pkm.store.registry.connect(Settings()). Capture: total cost_usd to date, total tokens, and the per-month breakdown.

    Fill the PROGRESS.md "Cost Actuals" table:
    - Infrastructure (recurring): $0/mo — actual (Turso free tier, CF Workers free tier, no recurring infra)
    - GitHub Actions minutes: $0 — actual (public repo = unlimited free; verified GUARD-04)
    - Cloudflare Workers: $0 — actual (free tier; Workers AI within 10K/day free limit at current scale, per Phase 6)
    - Turso: $0 — actual (free tier)
    - Claude API / OpenAI API (pipeline): replace "TBD $/mo (capped)" with the real $/mo figure(s) derived from agent_runs.cost_usd, plus the cumulative-to-date total and token count. State the per-run cap (PKM_RUN_COST_CAP_USD default 0.50) and OpenAI monthly hard limit (GUARD-05, set 2026-06-21) as the cost-control confirmation. Note the backend is OpenAI gpt-5.4-mini (T1-02), so the row labelled "Claude API" is satisfied by the OpenAI actual.

    Add a short "MVP-06 cost evidence" paragraph beneath the table citing the exact SQL run (SUM(cost_usd) FROM agent_runs) and the date queried. Record NO secret values — only dollar figures and token counts (token counts are not secrets).
  </action>
  <verify>
    <automated>cd /Users/RohitGupta/code/pkm-engine && grep -A8 "## Cost Actuals" PROGRESS.md | grep -cE "\\\$0|\\\$[0-9]" </automated>
  </verify>
  <acceptance_criteria>
    - The PROGRESS.md "Cost Actuals" table has no "—" or "TBD" cells remaining (`grep -A8 "## Cost Actuals" PROGRESS.md | grep -cE '—|TBD'` == 0)
    - The OpenAI row contains a real dollar figure derived from agent_runs (a number, not "TBD" or "—"), with the cumulative total and a per-month value
    - A literal SQL reference "SUM(cost_usd) FROM agent_runs" appears in the MVP-06 evidence paragraph
    - The four infra rows (GitHub Actions, Cloudflare Workers, Turso, Infrastructure recurring) each show $0
    - PKM_RUN_COST_CAP_USD and the OpenAI monthly hard limit are both referenced as cost-control confirmation
    - `grep -iE "sk-|TURSO_TOKEN|OPENAI_API_KEY|PKM_KEY|VAULT_PAT" PROGRESS.md` returns nothing (no secret values committed)
    - T1-02 condition 2 verified: `grep -n "compute_cost" pkm/llm/client.py` returns >= 1 hit at the cost_usd assignment (not hardcoded 0.0)
  </acceptance_criteria>
  <done>PROGRESS.md Cost Actuals table filled with $0 infra (4 lines) + real OpenAI $/mo from agent_runs.cost_usd + per-run and monthly caps referenced; no secrets; T1-02 condition 2 verified.</done>
</task>

<task type="auto">
  <name>Task 2: Tier-1 batch MVP-gate review in DECISIONS.md</name>
  <files>DECISIONS.md</files>
  <read_first>
    - DECISIONS.md "Tier-1 Batch (for MVP gate review)" section (T1-01 and T1-02 full text, including T1-02's three locked conditions)
    - pkm/llm/pricing.py and pkm/llm/client.py line ~352 (verify condition 2: cost_usd from compute_cost)
    - .github/workflows/ingest.yml (verify condition 1: timeout-minutes: 10, no Batch API call, standard sync Chat Completions)
    - pkm/llm/client.py _make_input_hash (verify condition 3: model string in cache key — cache-bust accepted)
    - .planning/REQUIREMENTS.md (the v2/Out-of-Scope table — to confirm no Tier-1 item has silently drifted into V1 scope)
  </read_first>
  <action>
    Append a "Phase 8 MVP-gate review" section to DECISIONS.md under the Tier-1 Batch heading. For each Tier-1 item, record a disposition (reaffirm / revise / defer-to-V1) with concrete verification:

    T1-01 (Vectorize over Turso native vectors): disposition reaffirm (or revise if Turso native vectors reached parity — they have not). Cite that Vectorize is live (`pkm-claims` index, 160 claims embedded, Phase 6 Wave 3) and the edge query worker depends on it. No change at MVP gate.

    T1-02 (OpenAI gpt-5.4-mini backend): verify each of the three locked conditions and record PASS/FAIL with file:line evidence:
    1. Dispatch = sync API, not Batch — verify .github/workflows/ingest.yml has `timeout-minutes: 10` and that pkm/llm/client.py uses the synchronous Chat Completions endpoint (no Batch API). Record the line.
    2. cost_usd computed from usage — verify pkm/llm/client.py calls pkm.llm.pricing.compute_cost (line ~352) and that pricing.py fails loud on unknown model (KeyError, never 0.0). Record the line. Cross-reference the MVP-06 cost actuals recorded in PROGRESS.md (Task 1) as proof the computed costs are non-zero and real.
    3. Cache-bust accepted — verify _make_input_hash includes the model string (pkm/llm/client.py lines ~27-29 per T1-02 text) and note that the one-time full re-ingest has occurred (the 160-claim corpus reflects post-switch ingest).

    Also scan DECISIONS.md and STATE.md for any Tier-1-class decisions accumulated since Phase 1 that are NOT yet in the Tier-1 Batch section (e.g. the T2-05-04 drop-FK / free-text-provenance deferral, which T2-05-04 explicitly says "is deferred to MVP review as a Type-1 contract change"). If found, list them in this section as "carried to MVP review" with a one-line disposition. Do NOT lock any new Type-1 decision here — that is the human's call at the Plan 03 MVP-review checkpoint.

    Record NO secret values. Reference file:line evidence, not pasted secrets.
  </action>
  <verify>
    <automated>cd /Users/RohitGupta/code/pkm-engine && grep -c "Phase 8 MVP-gate" DECISIONS.md && grep -c "T1-01" DECISIONS.md && grep -c "T1-02" DECISIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 8 MVP-gate" DECISIONS.md` >= 1 (new section added)
    - The section contains an explicit disposition for T1-01 (reaffirm/revise) with Vectorize live-state evidence
    - The section contains PASS/FAIL for each of T1-02's three conditions, each with a file:line citation (ingest.yml timeout, client.py compute_cost line, client.py _make_input_hash line)
    - Any Tier-1-class item accumulated since Phase 1 not already in the batch (e.g. the T2-05-04 drop-FK deferral) is listed as "carried to MVP review"
    - `grep -iE "sk-|TURSO_TOKEN|OPENAI_API_KEY|PKM_KEY|VAULT_PAT|x-access-token" DECISIONS.md` returns nothing
    - No new Type-1 decision is locked in this section (language is "carried to MVP review" / "disposition: reaffirm", not "locked")
  </acceptance_criteria>
  <done>DECISIONS.md "Phase 8 MVP-gate review" section records T1-01 disposition + T1-02 three-condition verification with file:line evidence + any carried Tier-1 items; no secrets; no new Type-1 lock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → Turso | cost query reads agent_runs via TURSO_TOKEN |
| PROGRESS.md / DECISIONS.md → public repo | both files are committed to public pkm-engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information disclosure | PROGRESS.md / DECISIONS.md | mitigate | Record only dollar figures, token counts, and file:line citations. Acceptance criterion greps for secret patterns (sk-, TURSO_TOKEN, OPENAI_API_KEY, PKM_KEY, VAULT_PAT, x-access-token) and must return nothing. |
| T-08-05 | Integrity | cost actuals | mitigate | Derive OpenAI $/mo from real agent_runs.cost_usd via the existing _running_totals SQL pattern — not an estimate. Verify T1-02 condition 2 (compute_cost, not hardcoded 0.0) before recording; if it fails, STOP and surface as Mode-C blocker rather than recording fake zeros. |
| T-08-06 | Elevation of privilege | Tier-1 dispositions | mitigate | This plan records dispositions and condition verification only; it does NOT lock any new Type-1 decision. The MVP-ready / V1-advancement call is a human judgment reserved for the Plan 03 checkpoint. Acceptance criterion forbids "locked" language. |
| T-08-SC | Tampering | npm/pip installs | accept | No package installs — pure doc + SQL-query work reusing existing helpers. No supply-chain surface. |
</threat_model>

<verification>
- PROGRESS.md Cost Actuals table: no "—"/"TBD" cells; 4 infra rows = $0; OpenAI row = real $/mo from agent_runs; no secrets
- DECISIONS.md "Phase 8 MVP-gate" section: T1-01 disposition + T1-02 three-condition PASS/FAIL with file:line evidence; no new Type-1 lock
- T1-02 condition 2 verified at the code level (compute_cost, not 0.0) — load-bearing for MVP-06 cost evidence integrity
</verification>

<success_criteria>
- MVP-06 cost actuals recorded in PROGRESS.md with real agent_runs-derived OpenAI spend and $0 infra (MVP-06 PASS from automated evidence)
- Tier-1 batch (T1-01, T1-02) reviewed with dispositions and condition verification recorded in DECISIONS.md
- No V1/V2/V3 work introduced; no new Type-1 decision locked autonomously
</success_criteria>

<output>
Create `.planning/phases/08-hardening-mvp-gate/08-02-SUMMARY.md` when done
</output>